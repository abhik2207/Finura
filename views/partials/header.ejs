<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Finura</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="styles.css">
</head>

<body>
    <nav class="w-full flex items-center justify-between px-32 h-16">
        <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
        <h3 class="text-xl cursor-pointer">Finura</h3>
        <% if (loggedin) { %>
            <div class="flex gap-6">
                <a href="/shop" class="cursor-pointer">Shop</a>
                <a href="/cart" class="cursor-pointer">Cart</a>
                <a href="/account" class="cursor-pointer">My Account</a>
                <a href="/users/logout" class="text-red-500">Logout</a>
            </div>
        <% } %>
    </nav>